node_modules
out
release
dist
*.log
.DS_Store
data

# ONNX-Modell (99 MB) – nicht versioniert, lokal ablegen (siehe README)
resources/models/*.onnx
*.tsbuildinfo

# Lokales Build-/Venv-Verzeichnis für das ONNX-Modell
.model-build/

# Benchmark-Ground-Truth aus manuellen Boundary-Korrekturen: enthält
# kundenabgeleitete Geometrien und darf nie eingecheckt werden.
# (Das Exportskript verweigert Pfade im Worktree — dies ist das zweite Netz.)
*.local.json
